# 🔧 Исправление админки

## Проблема
Админка не добавляет букеты и допы - скорее всего база данных не настроена или есть проблемы с RLS политиками.

## Решение

### Шаг 1: Настройка базы данных

1. Откройте Supabase Dashboard: https://supabase.com/dashboard/project/cdtoxtctdpstjeikcurf

2. Перейдите в **SQL Editor** (левое меню)

3. Создайте новый запрос и вставьте содержимое файла `supabase-setup-complete.sql`

4. Нажмите **Run** (или F5)

5. Дождитесь выполнения (должно быть "Success. No rows returned")

### Шаг 2: Добавление тестовых данных (опционально)

1. В SQL Editor создайте новый запрос

2. Вставьте содержимое файла `sample-data.sql`

3. Нажмите **Run**

### Шаг 3: Проверка таблиц

1. Перейдите в **Table Editor** (левое меню)

2. Убедитесь, что созданы таблицы:
   - ✅ products
   - ✅ addons
   - ✅ orders
   - ✅ profiles
   - ✅ reviews
   - ✅ favorites

### Шаг 4: Проверка RLS политик

1. Откройте любую таблицу (например, products)

2. Нажмите на иконку щита (RLS) справа

3. Убедитесь, что политики включены и созданы:
   - "Anyone can view products"
   - "Authenticated users can insert products"
   - "Authenticated users can update products"
   - "Authenticated users can delete products"

### Шаг 5: Временное отключение RLS (для тестирования)

Если админка всё ещё не работает, временно отключите RLS:

```sql
-- ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ! НЕ ИСПОЛЬЗУЙТЕ В ПРОДАКШЕНЕ!
ALTER TABLE products DISABLE ROW LEVEL SECURITY;
ALTER TABLE addons DISABLE ROW LEVEL SECURITY;
ALTER TABLE orders DISABLE ROW LEVEL SECURITY;
```

После этого попробуйте добавить букет через админку.

### Шаг 6: Проверка в браузере

1. Откройте http://localhost:3000/admin

2. Перейдите на вкладку "Букеты"

3. Нажмите "Добавить букет"

4. Заполните форму:
   - Название: "Тестовый букет"
   - Цена: 2500
   - Тип букета: Монобукет
   - Цвет: Красный

5. Нажмите "Сохранить"

6. Откройте консоль браузера (F12) и проверьте ошибки

### Шаг 7: Проверка ошибок

Если букет не добавляется, откройте консоль браузера (F12) и посмотрите на ошибки:

**Типичные ошибки:**

1. **"new row violates row-level security policy"**
   - Решение: Выполните SQL из Шага 5 (отключите RLS)

2. **"permission denied for table products"**
   - Решение: Проверьте, что вы используете правильный API ключ в `.env.local`

3. **"relation 'products' does not exist"**
   - Решение: Выполните `supabase-setup-complete.sql` заново

4. **"null value in column 'slug' violates not-null constraint"**
   - Решение: Уже исправлено в коде - slug теперь генерируется автоматически

## Быстрая проверка через SQL

Выполните в SQL Editor:

```sql
-- Проверка, что таблицы созданы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';

-- Проверка, что можно добавить букет
INSERT INTO products (name, price, bouquet_type, color)
VALUES ('Тестовый букет', 2500, 'mono', 'red')
RETURNING *;

-- Проверка, что букет добавился
SELECT * FROM products;

-- Удаление тестового букета
DELETE FROM products WHERE name = 'Тестовый букет';
```

Если все запросы выполнились успешно - база данных настроена правильно!

## Проверка через Postman/curl

Проверьте API напрямую:

```bash
curl -X POST 'https://cdtoxtctdpstjeikcurf.supabase.co/rest/v1/products' \
-H "apikey: YOUR_ANON_KEY" \
-H "Authorization: Bearer YOUR_ANON_KEY" \
-H "Content-Type: application/json" \
-d '{
  "name": "Тестовый букет",
  "price": 2500,
  "bouquet_type": "mono",
  "color": "red"
}'
```

Замените `YOUR_ANON_KEY` на ключ из `.env.local`

## Всё ещё не работает?

1. Проверьте `.env.local` - правильные ли ключи?
2. Перезапустите dev сервер: `npm run dev`
3. Очистите кэш браузера (Ctrl+Shift+Delete)
4. Проверьте Network tab в DevTools - какой статус у запроса?

## Контакты для помощи

Если ничего не помогло, отправьте скриншот:
1. Консоли браузера (F12 → Console)
2. Network tab (F12 → Network → запрос к Supabase)
3. SQL Editor с результатом выполнения скрипта
